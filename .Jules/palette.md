## 2024-05-01 - Add ARIA Labels to close buttons
**Learning:** Icon-only close buttons (like "✕") are often implemented without proper `aria-label` attributes, making them inaccessible to screen readers.
**Action:** Always verify that buttons containing only an icon or symbol have an explicit `aria-label` attribute describing their action (e.g., "Close").
